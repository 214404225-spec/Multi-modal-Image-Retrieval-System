"""
测试数据定义

查询按路由路径分组（与 pipeline.py 路由逻辑一致）：
- needs_vl（物体数量 或 非CLIP友好属性）→ VL 精排（CLIP粗排 + VL验证）
- 非 needs_vl → CLIP-only 常规检索
- need_retrieval=False → 直接回复

CLIP友好属性：颜色/大小/明暗（白色、黑色、棕色、红色、蓝色、绿色、
黄色、灰色、明亮、昏暗、大、小）—— CLIP 可直接处理，无需 VL。
其他属性（姿态、场景、构图、视角、清晰度等）为复杂属性，需 VL 验证。
"""

TEST_QUERIES = [
    # ============================================================
    # VL 精排 — needs_vl（物体数量 或 复杂属性），10 条
    # ============================================================

    # 物体数量（计数需 VL 验证）
    "帮我找2张3只猫的图片",              # 计数 only
    "找3只白色的猫",                     # 计数 + 简单属性（合并验证）
    "帮我找一只棕色的狗",                # 计数 + 简单属性
    "找3张2只猫的照片",                  # 计数 only

    # 复杂属性（姿态/场景/构图/视角/清晰度）
    "帮我找10张狗的全身图片",             # 构图属性
    "找很多站立的熊",                     # 姿态属性 + 模糊数量
    "检索3张室外的汽车图片",             # 场景属性
    "给我看一些清晰的键盘照片",           # 清晰度属性 + 模糊数量
    "帮我找正面的猫的图片",              # 视角属性
    "找5只站立的熊",                     # 计数 + 复杂属性（合并验证）

    # ============================================================
    # CLIP-only — 无复杂属性无计数（简单属性 或 无属性），10 条
    # ============================================================

    # 简单属性（颜色/大小/明暗 → CLIP 直接编码）
    "帮我找白色的狗",
    "找棕色的猫",
    "找大的狗",
    "显示黑色的熊",
    "找明亮的室内图片",

    # 无属性（常规检索）
    "帮我找狗的图片",
    "搜索包含熊的图像",
    "我想看大象",
    "检索椅子的照片",
    "找猫的图片",

    # ============================================================
    # 不需要检索 — 直接回复，5 条
    # ============================================================

    "今天的天气真好，不需要找图",
    "你好，我只是来打个招呼",
    "谢谢你的帮助，我不需要图片",
    "今天心情不错，无关图像",
    "不用找了，我改变主意了",
]
